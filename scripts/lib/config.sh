#!/usr/bin/env bash

###############################################################################
# config.sh
###############################################################################

CONFIG_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")"/../../config && pwd)"

source "${CONFIG_DIR}/default.conf"
